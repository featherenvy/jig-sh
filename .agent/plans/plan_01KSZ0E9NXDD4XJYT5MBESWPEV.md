Address architecture findings from the CLI entrypoint through runtime. Plan: split command DTOs by family, split oversized CLI run responsibilities into prompt/vault/bootstrap modules, route prompt operations through neutral runtime DTOs, extract manifest tool execution from runtime.rs, introduce typed response construction for selected high-churn outputs, then run local checks and comprehensive review.