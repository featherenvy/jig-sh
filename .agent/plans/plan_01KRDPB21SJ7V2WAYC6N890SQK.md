Phase 1: implement scoped fixes for confirmed hotspots: bootstrap template identity, work command routing duplication, plan/session event modeling, managed-path policy separation, and bootstrap test cost. Phase 2: run fresh-eyes review, fix findings, and re-run relevant verification.

Implemented all five validated hotspots. Bootstrap template source identity now flows through TemplateIdentity while preserving legacy .jig.yml fields. Work MCP calls deserialize typed request structs and preserve null-default tolerance for legacy clients. Plan/session events are typed enums with legacy JSONL serialization/deserialization and append now rejects closed plans. Managed path policy moved into bootstrap/managed_paths.rs. Bootstrap test fixture materialization copies only templates/, reducing the bootstrap suite to about 5-6 seconds locally. Fresh-eyes review reported no findings; residual null-default edge was fixed. Verification: cargo test -p jig-sh, make fmt-check, make clippy, scripts/jig work check, scripts/jig work gates, and git diff --check all pass.